character: 爱
parts:
  - strokes
  - pillars
  - plate

enable_pillars: true
enable_plate: true

# untilted_mode: true
# to_bottom_mode: true

debug_options:
  enable_plot: false
  # plot_window_zoom: 2
  plot_medians: true
  plot_stroke: true
  plot_voronoi: true
  # plot_orig_medians: true

scale: 1.4

general_options:
  parts_per_stroke_unit: 80
  distance_between_strokes: 0
  thickness: 90
  stretch: 100
  # smoothen_surface_amount: 40

flat_mode_options:
  spacing: 10

pillar_options:
  thickness: 14
  insert_multiplier: 1
  insert_n_segments: 20
  # insert_margin:
  insert_angle: 0
  plate_insertion: 0.2

plate_options:
  height: 60

per_stroke_options:
  5:
    pillar_options:
      pillar_end_distance: 0.9
  6:
    pillar_options:
      pillar_end_distance: 0.8
  7:
    pillar_options:
      pillar_end_distance: 0.7
  8:
    pillar_options:
      pillar_end_distance: 0.3
  9:
    pillar_options:
      pillar_end_distance: 0.75
